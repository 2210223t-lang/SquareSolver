#ifndef TRIAL_TERMINAL_H
#define TRIAL_TERMINAL_H
#include <stdio.h>

/**
 * @brief Run a program that parses command line arguments and run's mode which them calls
 *
 * @param argc Amount of commands
 *
 * @param argv Array that stores all commands
 */
int TerminalCommands( int argc, char* argv[] );

#endif // TRIAL_TERMINAL_H
